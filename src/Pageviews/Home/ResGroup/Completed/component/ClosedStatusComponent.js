import { Typography } from "@mui/material";
import React from "react";

const ClosedStatusComponent = ({ props }) => {
  return (
    <Typography sx={{ fontWeight: "500" }}>Status is coming...</Typography>
  );
};

export default ClosedStatusComponent;
